#!/bin/sh
# Launcher xcg-proxy. Delegates to _xcg-launcher (shared with xcg-config).
# See _xcg-launcher for path resolution and dev/packaged detection logic.

exec "$(dirname "$0")/_xcg-launcher" xcg-proxy "$@"
